import { SectionCard } from "@/components/ui/SectionCard";

export const ActionCard = ({ title, description, actions }) => (
  <SectionCard title={title} description={description} actions={actions} />
);
